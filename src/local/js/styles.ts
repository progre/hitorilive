import { CSSProperties } from 'react';

export const verticalFlexStyle: CSSProperties = {
  display: 'flex',
  flexDirection: 'column',
};

export const grayBlock: CSSProperties = {
  backgroundColor: '#eee',
  padding: 10,
};
